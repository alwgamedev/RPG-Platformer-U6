using UnityEngine;

namespace RPGPlatformer.SceneManagement
{
    public class PlayerSpawnPoint : MonoBehaviour
    {
        //[SerializeField] string id;
        ////we could also just use the game object name or we could generate a uid
        ////but this is fine

        //public string ID => id;
    }
}